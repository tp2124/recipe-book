# recipe-book
An application to accept, store, and display recipes for cooking. This is a practice application to work with Entity Framework. 

RecipeBookServer = Generic backend endpoints
Recipe.Data = Data models for storing content (Could be abstracted out in order to allow for non-Entity Framework representations).
RecipeBook = Website front end
RecipeBook.LocalClient = WPF Local application to interface with the data